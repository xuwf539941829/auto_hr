# 基于“画像对齐-证据审计”的双闭环 AI 招聘专家系统 —— 架构与设计说明书

## 1. 系统概述
本系统致力于解决传统 AI 招聘自动化中“JD 标准刻板”、“缺乏人工干预”以及“AI 判卷不可视”等痛点。通过引入“双闭环”设计，将机械的自动化工具升级为可伴随人类专家学习、成长的“AI 招聘专家系统”。

系统核心特色：
*   **画像初建与动态对齐（第一闭环）：** 不再仅按死板的 JD 找人，而是由 AI 翻译 JD 生成多维度“胜任力逻辑模型”，并在前端提供动态权重调节与自然语言指令纠偏，形成真正的“老板标准”。
*   **证据审计引擎与人工干预（第二闭环）：** AI 筛选不再是黑盒，必须提供基于简历原文的“证据清单”（动作数据、时间轴事实等）。所有被抓取的简历统一入库留存，用户可通过可视化的简历墙对任何结果进行**手动收藏/打招呼**，并提交**负反馈**让系统自适应更新下一次抓取标准。

## 2. 核心架构组成
系统采用轻量级的 **前端可视化层 + 本地数据持久层 + 后台爬取决策执行层** 分离架构。

### 2.1 Web UI 可视化控制台 (`app.py`)
使用 Streamlit 构建的轻量级 Web 交互界面。
*   **Phase 1 & 2：战略制定（画像对齐）**
    *   接收文本格式的原始口语化 JD，调用智谱大模型(GLM-4)进行深度结构化转译。
    *   展示转译后的画像初稿（包含显性要求、隐性要求及权重分布）。
    *   提供意图修改框与权重调节器，用户可输入纠偏指令（如“不要经验太长，要年轻野心大”），生成《最终执行画像》并保存到本地数据库。
*   **Phase 3 & 4：战果复核与纠偏（证据审计闭环）**
    *   读取数据库中所有候选人（含 S 级、A 级以及 REJECTED 淘汰者），以“简历池”卡片形式展示。
    *   为每位候选人展示 AI 提取的具体动作《证据清单》。
    *   提供 **人工审批通道**：用户可点击“手动发起打招呼”或“手动收藏”，这些请求将被送入操作队列供后台执行。
    *   提供 **负反馈机制**：允许用户填写拒绝理由。反馈内容将反向附加到 `JobProfile` 的标准中，指导下一轮 AI 筛选。

### 2.2 数据持久化模块 (`database.py`)
采用轻量级 SQLite，实现前后台系统状态解耦及数据留存，确保每份简历被追踪。
包含以下核心数据表：
*   `JobProfile`：保存最新的岗位执行画像结构数据。前端写入，后台读取。
*   `ResumeAudit`：记录后台引擎扫描的所有简历信息，包括姓名、AI 得分、提取的证据清单(`evidence_list`)、评定等级(`status`: S/A/REJECTED) 和原始数据备份。前端从中读取用于展示。
*   `ManualActionQueue`：人工操作指令队列。存放用户在前端点击的“手动打招呼”或“手动收藏”任务，后台轮询消费。

### 2.3 自动化驱动与 AI 审计引擎 (`webBossAI.py`)
继承 Playwright 无头浏览器的核心自动化能力，并在此基础上进行了“大脑重构”。
*   **配置自适应读取：** 在无限轮询的每一次外层循环（1-50页抓取前），动态从 SQLite 的 `JobProfile` 表中获取最新的画像标准。一旦标准在前端被修改，下一轮即刻生效。
*   **证据审计筛选引擎：**
    *   抛弃旧版基于固定规则的布尔值筛选，全面升级为“证据审计双闭环版” Prompt。
    *   引入 **白帽审计（事实逻辑）** 与 **红帽审计（价值观推断）**，严抓简历中的“动作数据”，忽略虚词。
    *   按照 90 分（S级 - 自动收藏+打招呼），80-89 分（A级 - 自动收藏），80分以下（淘汰）进行硬分级操作，并将全量结果落库。
*   **人工指令消费：** 在每一轮扫描间隙，监听并消费来自 `ManualActionQueue` 的人工手动下达的收藏和沟通任务。

## 3. 业务流转流程图解
1.  **【启动】** -> 启动 `database.py` 创建 SQLite -> 启动 `app.py` Web服务 -> 启动 `webBossAI.py` 驻留后台。
2.  **【人机对齐】** -> 用户通过 Web UI 输入 JD -> AI 提取画像 -> 用户微调校准后确认 -> 保存到 `JobProfile`。
3.  **【主动搜寻】** -> `webBossAI` 读取 `JobProfile` -> 自动翻页获取简历 -> 交由 AI 根据白帽/红帽标准进行**证据审计** -> 落库到 `ResumeAudit`。
4.  **【触发动作】** -> 判定 S 级（调用 Boss API 打招呼+收藏），A 级（调用 Boss API 收藏）。
5.  **【人工闭环】** -> 用户登录 Web UI 查看简历墙 -> 查看 AI 的判卷证据。
    *   若发现遗漏好苗子（AI判为 Rejected，但人觉得好） -> 用户点击“手动打招呼” -> 指令写入队列 -> `webBossAI` 下一轮消费该指令进行补救。
    *   若发现抓的人“对味不对” -> 用户填入负面吐槽并提交 -> AI 将吐槽转化为新的标准附加到 `JobProfile` -> `webBossAI` 下一次循环采用新标准。